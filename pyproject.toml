[project]
name = "galim-pro-mqtt"
version = "0.1.0"
description = "Publish Galim Pro homework to Home Assistant over MQTT"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

